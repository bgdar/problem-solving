#include <cstdarg>
#include <map>
#ifndef SURJECTION_&_BIJECTION_H
#define SURJECTION_&_BIJECTION_H

struct typeSurjection{
  char message[50] ;// message 


};
/**
 * return
 */
typeSurjection surjection(int domain[],char kodomain[]){


  int panjang_domain = sizeof(domain) / sizeof(domain[0]);
  int panjang_kodomain = sizeof(kodomain) / sizeof(kodomain[0]);

  if (panjang_domain >= panjang_kodomain) {
    
  }

  for(int i = 0 ; i <= panjang_array;i++){
  };



}


#endif // !SURJECTION_&_BIJECTION_H
#define SURJECTION_&_BIJECTION_H

struct typeSurjection{
  char message[50] ;// message 


};
/**
 * return
 */
typeSurjection surjection(int domain[],char kodomain[]){


  int panjang_domain = sizeof(domain) / sizeof(domain[0]);
  int panjang_kodomain = sizeof(kodomain) / sizeof(kodomain[0]);

  if (panjang_domain >= panjang_kodomain) {
    
  }

  for(int i = 0 ; i <= panjang_array;i++){
  };



}




